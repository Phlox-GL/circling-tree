# Circling tree migration follow-up

- Pinned the merged Phlox and current Calcit dependency inputs and declared the project version.
- Updated CI to install Yarn before compilation, validate canonical Snapshot formatting, and deploy only from main.
- Verified dependency resolution, immutable install, check-only, JS generation, and Vite production build.
